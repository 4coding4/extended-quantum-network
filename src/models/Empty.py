class Empty:
    """
    This class is used to replace the combined class, and provide no models.


    Instructions
    ------------
    1. Import this class into the network
        from src.models.Empty import Empty

    2. Connect the models to the network (in specific to the QuantumChannel).
        models = Empty.empty_models
    """
    empty_models: dict = dict()
